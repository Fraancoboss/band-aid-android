<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".BandAidApplication"
        android:label="@string/app_name"
        android:theme="@style/Theme.BandAId">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.medicine.MedicineDetailActivity" />
        <activity android:name=".ui.calendar.CalendarActivity" />
        <activity android:name=".ui.user.UserSetupActivity" />
        <activity android:name=".ui.search.MedicineSearchActivity" />
        <activity android:name=".ui.search.MedicineCreateActivity" />

    </application>
</manifest>
