# Siguientes Pasos Tecnicos

1. Configurar el Android SDK mediante ANDROID_HOME o local.properties (requisito para ejecutar v0.1).
2. Validar la compilacion con .\gradlew assembleDebug (estado esperado con SDK configurado).
