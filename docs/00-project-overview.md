# Vision General del Proyecto

Band-AId es una aplicacion Android de un solo modulo con un unico punto de entrada en su estado actual.

Utiliza layouts XML con ViewBinding y no emplea Jetpack Compose.

No es una biblioteca, no es un proyecto multi-modulo y no incluye componentes de backend.

Estado actual: la compilacion falla porque el SDK de Android no esta configurado.
